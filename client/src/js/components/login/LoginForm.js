import React from 'react';

const LoginForm = () => {
    return (
        <form>
            <input type="text"/>
            <input type="password"/>
        </form>
    );
};

export default LoginForm;