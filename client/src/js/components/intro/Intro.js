import React from 'react';

import Buttons from './Buttons';

const Intro = () => {
    return (
        <div className="intro">
            <h1>Let's play with english!</h1>
            <Buttons />
        </div>
    );
};

export default Intro;